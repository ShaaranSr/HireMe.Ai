# HireMe.AI - Python Dependencies

# Core app
streamlit>=1.36.0
python-dotenv>=1.0.1

# LangChain + LangGraph + Gemini
langchain>=0.2.14
langgraph>=0.2.18
langchain-google-genai>=1.0.5

# Optional MCP integration (tools via MCP). Configure separately.
langchain-mcp-adapters>=0.0.7

# File parsing (resume uploads)
pypdf>=4.2.0
python-docx>=1.1.0
PyMuPDF>=1.24.10
pdfminer.six>=20240706
pdf2image>=1.17.0
pytesseract>=0.3.13

# Pydantic v2 is standard; LangChain still ships a v1 compatibility layer
pydantic>=2.7.0
